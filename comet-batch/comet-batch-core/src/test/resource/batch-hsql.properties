# Placeholders batch.*
#    for HSQLDB:
batch.jdbc.driver=org.h2.Driver
batch.jdbc.url=jdbc:h2:mem:DBName0;DB_CLOSE_DELAY=-1
# use this one for a separate server process so you can inspect the results
# (or add it to system properties with -D to override at run time).
# batch.jdbc.url=jdbc:hsqldb:hsql://localhost:9005/samples
batch.jdbc.user=sa
batch.jdbc.password=
batch.jdbc.testWhileIdle=false
batch.data.source.init=true
batch.database.incrementer.class=org.springframework.jdbc.support.incrementer.HsqlMaxValueIncrementer
batch.database.incrementer.parent=columnIncrementerParent
batch.grid.size=2
batch.verify.cursor.position=true
batch.jdbc.validationQuery=SELECT 1 FROM INFORMATION_SCHEMA.SYSTEM_USERS


# Placeholders batch.*
#    for HSQLDB:
#batch.jdbc.url1=jdbc:h2:mem:DBName1;DB_CLOSE_DELAY=-1
# use this one for a separate server process so you can inspect the results
# (or add it to system properties with -D to override at run time).
# batch.jdbc.url=jdbc:hsqldb:hsql://localhost:9005/samples
#batch.jdbc.user1=sa
#batch.jdbc.password1=

